# --- FASTAPI STACK ---

# FastAPI: framework moderno y rápido para construir APIs con Python.
fastapi==0.103.1

# Uvicorn: servidor ASGI ligero y rápido para ejecutar aplicaciones FastAPI.
uvicorn==0.23.2


# --- DATABASE & ORM ---

# SQLAlchemy: ORM completo y flexible para trabajar con bases de datos SQL.
sqlalchemy==2.0.21

# PyMySQL: cliente MySQL puro en Python, usado por SQLAlchemy para conectar con MySQL/MariaDB.
pymysql==1.1.0


# --- SECURITY & AUTHENTICATION ---

# Cryptography: biblioteca de cifrado y firmas digitales (soporte requerido por python-jose).
cryptography==41.0.4

# python-jose: implementación de JWT en Python, compatible con cryptography.
python-jose[cryptography]==3.3.0

# Passlib: biblioteca para hashing seguro de contraseñas (incluye bcrypt).
passlib[bcrypt]==1.7.4

# Bcrypt: algoritmo robusto de hashing, requerido por passlib.
bcrypt>=4.0.1


# --- PRODUCTION DEPLOYMENT ---

# Gunicorn: servidor WSGI para despliegue en producción (aunque Uvicorn es preferido con FastAPI + ASGI).
gunicorn==21.2.0


# --- CODE QUALITY & TESTING ---

# Pylint: herramienta de análisis estático de código para mantener buenas prácticas y estilo limpio.
pylint==3.3.7

# Pytest: framework de testing flexible para tests unitarios y de integración.
pytest

# HTTPX: cliente HTTP asíncrono ideal para pruebas y llamadas a APIs externas.
httpx==0.27.0


# --- CONFIGURATION MANAGEMENT ---

# Python-dotenv: carga variables de entorno desde archivos `.env` al entorno de ejecución.
python-dotenv


# --- DATA VALIDATION ---

# Pydantic: validación de datos basada en anotaciones de tipo, versión 2.x con validadores extendidos.
pydantic[email]==2.5.3


# --- FORM HANDLING ---

# python-multipart: permite recibir formularios y archivos en FastAPI (para OAuth2, uploads, etc.).
python-multipart
